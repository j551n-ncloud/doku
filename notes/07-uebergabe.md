# 07 – Einführungsphase (Übergabe)

Du hast gesagt: die Übergabe passiert direkt nach dem Projekt.
Im LaTeX-Skelett `Inhalt/07-Einfuehrungsphase.tex` steht nur ein
Platzhalter. Trage hier deine Antworten ein, dann fülle ich's aus.

## 1. An wen wird übergeben?

- Welches Team / welche Personen genau übernehmen den PBS?
  > ANTWORT:

- Wer bekommt die Admin-Zugänge (PBS, API-Token, root)?
  > ANTWORT:

## 2. Was wird übergeben?

- Übergabe der Web-UI-Zugänge / LDAP-Berechtigungen
  > Status (geplant / erledigt / offen):

- Übergabe des root-Passworts (Vault? KeePass? Bitwarden?)
  > ANTWORT:

- Übergabe der API-Token-Geheimnisse
  > ANTWORT:

- Übergabe der Betriebsdokumentation (Confluence-Link?
  Git-Repo? Markdown im internen Wiki?)
  > ANTWORT:

## 3. Schulung / Einweisung

- Findet eine formale Einweisung statt? (Termin, Dauer)
  > ANTWORT:

- Inhalte der Einweisung (Stichpunkte):
  > ANTWORT:

## 4. Parallel-Betrieb mit der alten Lösung

- TSM/Tape-Sicherung bleibt weiter aktiv?
  > ANTWORT:

- Ceph-RBD-Backup wird abgeschaltet oder bleibt parallel?
  > ANTWORT:

- Wann (geplant) wird komplett auf PBS umgestellt?
  > ANTWORT:
